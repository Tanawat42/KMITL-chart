import { createTheme } from '@mui/material/styles';
import { css } from '@emotion/react'
const	palette = {
	background: {
		default: "#4a575e",
		paper: "#9fb4bf"
	},
	primary: {
		main: "#263842",
		light: "#fff"
	}
};

export default createTheme({
	palette
});
